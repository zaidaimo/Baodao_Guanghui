using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Destroy_Meat_Tsai : MonoBehaviour
{
    void Update()
    {
        Destroy(gameObject);
    }
    // Start is called before the first frame update
    public void Destroy()
    {
        Destroy(gameObject);
    }
}
